import 'package:flutter/material.dart';

class ErrorScreen extends StatelessWidget {
  const ErrorScreen({super.key});
  static const route = "/ErrorScreen";

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
