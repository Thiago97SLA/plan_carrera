import 'package:flutter/material.dart';

class Palette {
  // Background color
  static const backgraound = Color(0XFF223343);

  //Base colors
  static const primary = Color(0XFFfdf4af);
  static const secundary = Color(0XFFb9f5e9);
  static const tertiary = Color(0XFFfebcbc);
  static const nuetral = Color(0xFFFFFFFF);
}
